{
  "name": "@kakomimasu/client-js",
  "version": "1.0.0-alpha.1",
  "exports": "./src/index.ts",
  "tasks": {
    "generate": "openapi-generator-cli generate && deno fmt && deno lint --fix || deno check ."
  },
  "lint": {
    "rules": {
      "exclude": ["no-unused-vars"]
    }
  },
  "publish": {
    "exclude": [
      "!src",
      ".vscode",
      ".github",
      "template",
      "openapitools.json"
    ]
  }
}
